language: elixir
elixir:
  - 1.5.3
otp_release:
  - 20.0

services:
  - postgresql

env:
  - DB_HOST=localhost MIX_ENV=test

before_script:
  - psql -c "CREATE USER demo WITH PASSWORD 'pass';" -U postgres
  - psql -c "CREATE DATABASE demo;" -U demo
  - mix ecto.migrate
