language: elixir
elixir:
  - 1.5.3
otp_release:
  - 20.0

services:
  - postgresql

env:
  - DB_HOST=localhost MIX_ENV=test

before_script:
  - psql -c "CREATE DATABASE demo;"
  - psql -c "CREATE USER demo WITH PASSWORD 'pass';"
  - mix ecto.migrate
